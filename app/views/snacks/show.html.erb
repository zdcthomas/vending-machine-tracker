<%= @snack.name %>
<%= @snack.price %>

Locations
<% @machines.each do |machine| %>
  * <%= machine.location %>: <%= machine.snacks.count %> kinds of snacks</br>
  Average Price: $<%= machine.average_price %>
<% end %>